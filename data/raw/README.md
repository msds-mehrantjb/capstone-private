# Raw and Baseline Inputs

Baseline/sample inputs used to initialize or test audit workflows.

This directory contains scope templates/examples and baseline dashboard data. Files here should be treated as source/reference inputs; mutable audit state belongs under `data/work/<year>/`.

When adding a new scope template, use a clear year/name/version convention and keep the JSON schema compatible with the Scope & Context backend.
