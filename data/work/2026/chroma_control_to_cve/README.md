# Control-to-CVE Chroma Index

Persistent Chroma vector-index storage used by control/CVE retrieval and mapping workflows.

The UUID-named subfolder contains internal HNSW/segment files generated by Chroma.

Do not manually edit, rename, or partially delete index files. Rebuild the index through the application/RAG pipeline if it becomes stale or corrupted.
