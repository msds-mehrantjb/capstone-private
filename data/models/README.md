# Persisted Embedding Models/Data

Local serialized embedding artifacts used by retrieval workflows.

Current files include ISO 27002/local embedding pickle artifacts.

These files are generated binary data. Rebuild them through the RAG/embedding pipeline rather than editing them directly.
