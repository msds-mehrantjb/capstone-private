# Scanner Configuration

Configuration for the lab/network scanner.

- `targets.json` defines the subnet, hosts, credentials/connection information, and related scan settings used by scanner scripts.

Treat credentials as sensitive. Avoid committing real production secrets; use lab/test credentials or a safer secret-management approach for non-lab environments.
