# Documentation Images

Images embedded in repository documentation.

Current asset:

- `iso27001-dashboard.jpg` — dashboard preview displayed in the root `README.md`.

Prefer descriptive, stable filenames so Markdown references remain valid.
