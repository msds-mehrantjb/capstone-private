# Workstation Behavior Agent

PowerShell agent components for collecting workstation activity data.

## Files

- `collect_behavior.ps1` — behavior collection logic.
- `config.json` — agent configuration.
- `install_behavior_agent.ps1` — installation helper.
- `register_behavior_task.ps1` — scheduled-task registration.

Review configuration and permissions before deploying to any Windows endpoint.
