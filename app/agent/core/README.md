# Agent Core

Core execution layer for the agent subsystem.

- `runner.py` provides the runner/entry logic used to execute agent workflows.

Keep orchestration concerns here; environment-specific collection belongs in `../collectors/`.
